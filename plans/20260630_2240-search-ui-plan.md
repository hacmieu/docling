# Plan — Search UI với token count + DeepSeek

- [x] `search.html` — bảng kết quả + badge tokens
- [x] API context + synthesize
- [ ] Thêm mode `concept` (markdown search) nếu cần

```bash
uv run python workspace/ocr_pipeline/webapp/pg_app.py --port 8766
# Mở http://127.0.0.1:8766/search
```
