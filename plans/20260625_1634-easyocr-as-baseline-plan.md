# Plan Entry - EasyOCR Baseline Decision

## Decision

- Use `memory_tmp_vie_easyocr.db` as current baseline dataset for downstream evaluation.

## Next step

1. Apply the same extraction/benchmark queries to the active DB first.
2. Keep Tesseract DBs as reference set for regression checks.
