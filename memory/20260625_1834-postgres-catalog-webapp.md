# Memory Entry — PostgreSQL catalog webapp

- Thêm `workspace/ocr_pipeline/webapp/pg_app.py` — UI quản lý đọc Postgres.
- Port **8766**; SQLite viewer giữ port **8765**.
- UI: **table list** (phân trang 50) + **detail panel** khi click dòng.
- Lọc theo status, drive (`project/hth-shared-drive`), tìm path.
