# Memory Entry - Git Workflow

- Checked `git status -sb`: clean working tree.
- Current branch: `main`.
- Tracking branch: `origin/main`.
- Remote push target: `origin` -> `https://github.com/docling-project/docling`.

## Practical answer

- If you are making direct updates to current branch, push to `origin/main`.
- If you are developing a new change, create a feature branch from `main`, then push that branch and open a PR.
