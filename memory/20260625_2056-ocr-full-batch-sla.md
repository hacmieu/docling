# Memory Entry — Full OCR batch + Teable ENV

- `.env`: thêm `TEABLE_*` placeholder (chưa dùng).
- Full OCR `--all`: **437 PDF** trong batch này; **447 success** tổng HTH-Shared (gồm pilot 10).
- SLA batch: **4692s (~78 phút)**, TB **10.7s/file**, 0 lỗi.
- Còn **28** `cataloged` (không phải PDF — docx, m4a, …).
